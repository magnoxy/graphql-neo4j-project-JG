{
  "version": 2,
  "builds": [
    {
      "src": "main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
        "src": "/graphql(.*)",
        "dest": "src/main.ts",
        "methods": ["POST", "GET"]
    }
  ]
}
